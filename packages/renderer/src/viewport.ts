/**
 * The viewport is the display area for rendering a scene.
 */
export interface Viewport {
  /**
   * The display width, in pixels.
   */
  readonly width: number;

  /**
   * The display height, in pixels.
   */
  readonly height: number;
}
